supabase>=2.0.0
python-dotenv>=1.0.0
httpx>=0.27.0
requests>=2.31.0
beautifulsoup4>=4.12.0
trafilatura>=1.12.0
rapidfuzz>=3.6.0
langdetect>=1.0.9
torch>=2.0.0
transformers>=4.35.0
stanza
lxml
wikipedia>=1.4.0  # v4.8: entity enrichment (auto-fetch bio + image)
# NOTE: keybert, sentence-transformers, Sastrawi are OPTIONAL.
# context_worker gracefully skips them if not installed (try/except).
# For finetuning (Colab), install: pip install -r finetuning/requirements_finetune.txt