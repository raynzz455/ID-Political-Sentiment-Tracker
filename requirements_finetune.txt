# Requirements for finetuning — use LATEST versions
# Install: pip install -r requirements_finetune.txt
#
# IMPORTANT: Colab pre-installs torchao 0.10.0 which crashes peft.
# Fix: upgrade torchao to latest with: pip install --upgrade torchao
# Or just use the Colab script which handles this automatically.

torch>=2.1.0
torchao>=0.16.0
transformers>=4.40.0
peft>=0.10.0
scikit-learn>=1.4.0
numpy>=1.26.0
huggingface_hub>=0.20.0
